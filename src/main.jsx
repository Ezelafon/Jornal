import React,{useEffect,useState} from 'react';
import{createRoot}from'react-dom/client';
import{createClient}from'@supabase/supabase-js';
import'./style.css';

const supabase=createClient('https://wrtnltpzmjwwgchapkui.supabase.co','sb_publishable_hYp_Dz-JLJ8E8vK4Z1r2dg_BUJ7fmv2');

function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const submit=async(e)=>{e.preventDefault();setError('');const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setError('No pudimos ingresar. Revisá tus datos.');};const google=async()=>{setError('');const{error}=await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:window.location.origin}});if(error)setError('No pudimos iniciar con Google.');};return <main className="login"><div className="brand">Jornal</div><h1>Tu jornada, simple.</h1><p>Ingresá para registrar lo que hiciste hoy.</p><button className="google" onClick={google}>Continuar con Google</button><div className="or">o</div><form onSubmit={submit}><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Contraseña" value={password} onChange={e=>setPassword(e.target.value)} required/><button>Ingresar</button>{error&&<small>{error}</small>}</form></main>}

function App(){const[session,setSession]=useState(null);const[profile,setProfile]=useState(undefined);const[text,setText]=useState('');const[activities,setActivities]=useState([]);const[saving,setSaving]=useState(false);useEffect(()=>{supabase.auth.getSession().then(({data})=>setSession(data.session));const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>subscription.unsubscribe()},[]);useEffect(()=>{if(!session){setProfile(undefined);return}load()},[session]);async function load(){const uid=session.user.id;const{data:p}=await supabase.from('jornal_profiles').select('*').eq('user_id',uid).maybeSingle();setProfile(p||null);if(!p)return;const{data:a}=await supabase.from('jornal_activities').select('*').eq('user_id',uid).eq('activity_date',new Date().toISOString().slice(0,10)).order('created_at',{ascending:false});setActivities(a||[])}async function save(){if(!text.trim()||!profile)return;setSaving(true);const{error}=await supabase.from('jornal_activities').insert({user_id:session.user.id,original_text:text.trim(),status:'confirmed'});setSaving(false);if(!error){setText('');load()}}if(!session)return <Login/>;if(profile===undefined)return <main className="login"><div className="brand">Jornal</div><p>Ingresando…</p></main>;if(profile===null)return <main className="login"><div className="brand">Jornal</div><h1>Acceso no habilitado.</h1><p>Tu cuenta está autenticada, pero todavía no fue incorporada al equipo de Jornal.</p><button className="google" onClick={()=>supabase.auth.signOut()}>Volver</button></main>;const name=profile.full_name?.split(' ')[0]||session.user.email?.split('@')[0];return <main><header><div><span className="eyebrow">JORNAL</span><h2>Hola, {name}</h2></div><button className="exit" onClick={()=>supabase.auth.signOut()}>Salir</button></header><section className="capture"><p className="date">HOY · {new Intl.DateTimeFormat('es-AR',{weekday:'long',day:'numeric',month:'long'}).format(new Date())}</p><h1>¿Qué hiciste hoy?</h1><p className="hint">Contalo como se lo contarías a un compañero.</p><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Ej: Estuvimos en AESA Añelo inspeccionando 14 retráctiles de 9 a 15..."/><div className="actions"><button className="mic" type="button" disabled>🎙️ <span>Contar</span></button><button className="save" onClick={save} disabled={!text.trim()||saving}>{saving?'Guardando…':'Registrar actividad'}</button></div></section><section className="today"><div className="sectionTitle"><h3>Mis actividades de hoy</h3><span>{activities.length}</span></div>{activities.length===0?<div className="empty">Todavía no registraste actividades.</div>:activities.map(a=><article key={a.id}><div className="dot">✓</div><div><p>{a.original_text}</p><small>{new Date(a.created_at).toLocaleTimeString('es-AR',{hour:'2-digit',minute:'2-digit'})}</small></div></article>)}</section></main>}
createRoot(document.getElementById('root')).render(<App/>);